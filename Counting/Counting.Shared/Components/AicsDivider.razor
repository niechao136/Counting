<div class="@DividerClass"></div>

@code {

  [Parameter] public bool IsVertical { get; set; }
  [Parameter] public string Class { get; set; } = string.Empty;

  private string DividerClass => $"{Class} divider divider--{(IsVertical ? "vertical" : "horizontal")}";

}